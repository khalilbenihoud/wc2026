# Tasks: Country Header Contrast Fix

## Implementation

1. [x] Change `bg-brand-bg/80 backdrop-blur-md` to `bg-brand-bg` on the sticky header in `src/components/CountryPage.tsx` (line 84)
2. [ ] Verify visually that the header appears solid and no aurora bleed occurs
3. [ ] Verify Tournament page header is unaffected (no regression)
